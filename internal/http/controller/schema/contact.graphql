type Contact {
    id: ID!
    platform: String!
    url: String!
    icon: String
}

input CreateContactRequest {
    platform: String!
    url: String!
    icon: Upload!
}

input UpdateContactRequest {
    id: ID!
    platform: String
    url: String
    icon: Upload
}